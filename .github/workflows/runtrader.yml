name: RunTrader v0

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  schedule:
    - cron: "30 21 * * 1-5"

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "RunTrader v0 hello at $(date -u)"