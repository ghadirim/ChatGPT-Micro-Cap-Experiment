name: RunTrader PostClose

on:
  workflow_dispatch:
  schedule:
    - cron: "30 21 * * 1-5"   # 4:30pm ET, Mon–Fri
  push:
    branches: [ main ]

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RunTrader starter OK: $(date -u)"